import React from "react";
import BookingForm from "../components/BookingForm";

const Booking = () => {
  return (
    <div>
      <BookingForm />
    </div> // it includes the booking form details means the user input will be taken here accordingly work will be proceed
  );
};

export default Booking;
